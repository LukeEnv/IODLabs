import { Router } from "express";

const router = Router();

// Placeholder for future RESTful chat-related routes
router.get("/", (req, res) => {
  res.send("Chat routes are active");
});

export default router;
